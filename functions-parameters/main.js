const quarterValue = (originalValue) => {
        const quarter = originalValue / 4

        return quarter
}

// Invoke the function and store the return value
const solution = quarterValue(365)
// Log the return value
console.log(solution)